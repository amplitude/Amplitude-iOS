machine:
  xcode:
    version: 7.3.1
  environment:
    LANG: en_US.UTF-8

dependencies:
  pre:
    - bundle install
  override:
    - bundle exec pod repo update --silent
    - bundle exec pod install
